# Copy to secrets.tfvars (gitignored). Apply with: terraform apply -var-file=secrets.tfvars
neon_api_key = "napi_..."
neon_org_id  = "org-..."

backend_secrets = {
  GOOGLE_GENERATIVE_AI_API_KEY = "..."
}
