Template, not an app. Nothing here runs in place. When asked to "set up deploy
like mem-soda" for a new project: read README.md, copy the files it lists into
the target repos, and resolve every `SET:` comment. Don't add what README's
"Not included" section defers.
